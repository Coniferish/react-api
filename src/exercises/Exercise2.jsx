/*
Create a simple React component which should display:
- an integer, starting at 0, in an element with `id='value'`
- a button with `id='increment'` which increments `value` when you click it
- a button with `id='decrement'` which decrements `value` when you click it
*/

export default function Exercise2() {
    return (
        <h1>Exercise 2</h1>
    )
}
